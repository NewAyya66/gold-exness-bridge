services:
  - type: web
    name: gold-exness-bridge
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: TWELVE_DATA_KEY
        sync: false
